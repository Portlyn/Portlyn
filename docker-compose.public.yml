services:
  portlyn:
    image: ghcr.io/portlyn/portlyn:${PORTLYN_IMAGE_TAG:-latest}
    pull_policy: always
    build: null

  frontend:
    image: ghcr.io/portlyn/portlyn-frontend:${PORTLYN_IMAGE_TAG:-latest}
    pull_policy: always
    build: null
